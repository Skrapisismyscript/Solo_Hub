
task.spawn(function()
    repeat task.wait(0.5)
            local args = {
                [1] = 10,
                [2] = "dash"
            }
            
            game:GetService("ReplicatedStorage").Events.takestam:FireServer(unpack(args))
   until false
end)
